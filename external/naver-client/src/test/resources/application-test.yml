spring:
  config:
    activate:
      on-profile: test
  external:
    naver:
      url: https://openapi.naver.com
      headers:
        client-id: ${client-id}
        client-secret: ${client-secret}
