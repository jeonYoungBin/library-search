spring:
  config:
    activate:
      on-profile: local
  external:
    naver:
      url: https://openapi.naver.com
      headers:
        client-id: ${client-id}
        client-secret: ${client-secret}
